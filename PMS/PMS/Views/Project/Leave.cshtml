@model PMS.Models.CreateProject

@{
    ViewBag.Title = "Leave";
    Layout = "~/Views/Shared/_admin_MD_TL.cshtml";
}


<h3>Are you sure you want to leave " @Html.DisplayFor(model => model.pro_name) " project?</h3>
<div>
    <hr />
    <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.strt_date)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.strt_date)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.end_date)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.end_date)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.price)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.price)
            </dd>

        </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
        <div class="form-actions no-color">
            <input type="submit" value="Leave" class="btn btn-danger btn-lg" />
        </div>
        <br />
        <br />
        @Html.ActionLink("Back to List", "ListCurrentPMTL")
    }
</div>
