@model PMS.Models.projectAssign

@{
    ViewBag.Title = "View";
    Layout = "~/Views/Shared/_admin_MD_TL.cshtml";
}

<h2>View</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>projectAssign</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.name_dm)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.name_dm)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.project.cust_name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.project.cust_name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.user.Role)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.user.Role)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
